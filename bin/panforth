#!/usr/bin/env bash
set -euo pipefail

HERE="$(cd "$(dirname "$0")" && pwd)"
ROOT="$(cd "$HERE/.." && pwd)"

export PAN_TOOL="panforth"

exec "$HERE/node_modules/.bin/tsx"     "$ROOT/scripts/pan-tool.mjs"     "$@"
